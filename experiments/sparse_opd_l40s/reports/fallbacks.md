# Fallback and variation log

| Time (UTC) | Component | Planned | Actual | Reason | Scientific impact |
|---|---|---|---|---|---|
| 2026-07-30 | Python runtime | OS Python 3.11 | Stable uv-managed CPython 3.11 | Ubuntu 22.04 repository package is 3.11.0rc1 | None; models, loss, precision, and training configuration unchanged |
| 2026-07-30 | Teacher runtime | Unpinned stable SGLang >=0.5.8 | SGLang 0.5.8 + official PyTorch 2.9.1 cu128 | Latest SGLang 0.5.16 resolved PyTorch cu130, which cannot initialize with driver 570; driver was intentionally left unchanged | None to model, FP8, KV cache, loss, or precision; runtime version is within the required range |
| 2026-07-30 | SGLang static memory fraction | 0.82 | 0.90 after bounding the hybrid state pool | At 0.82/0.94 the default 512-request Mamba allocation left no KV tokens; at 0.98 with a corrected 32-request pool, KV allocation consumed the 2 GiB FlashInfer workspace. A 0.90 pool balances BF16 KV capacity with backend workspace | No model/precision/context change; cache capacity is benchmarked |
| 2026-07-30 | Hybrid linear-attention state pool | SGLang defaults | `max_running_requests=32`, `max_mamba_cache_size=32` | Deterministic inference disables radix cache in SGLang 0.5.8, whose Qwen3-Next fallback preallocates state for 512 requests and leaves no full-attention BF16 KV tokens on 48 GB; the planned benchmark has maximum batch 32 | Caps concurrency at the experiment's required maximum; does not change weights, context, or cache dtype |
| 2026-07-30 | LoRA adapter transport | Prefer NCCL broadcast | Explicit measured CPU staging between GPU 2 trainer and GPU 3 Transformers replica | CUDA runtime reports peer access disabled for every GPU pair; a one-process dedicated rollout replica cannot form a two-rank NCCL group without adding unvalidated process orchestration | Correctness preserved by SHA-256 equality and strict policy-version checks; sync latency is logged and staleness must remain zero |
| 2026-07-30 | Portable report runtime | Ubuntu Node.js 12 | Isolated official Node.js LTS 24.18.1 under `/opt/node-v24.18.1` | The packaged Data Analytics renderer uses modern JavaScript syntax unsupported by Ubuntu 22.04's Node 12 | No analytical impact; the canonical validator and renderer are unchanged |
| 2026-07-30 | Portable report browser QA | Chromium-assisted verification | Structural verification with semantic chart tables | No compatible Chromium/headless-shell is installed, and the report workflow explicitly does not download a browser during generation | Exact artifact/payload equality and semantic fallback pass; interactive desktop/narrow viewport and source-dialog behavior remain unverified |
